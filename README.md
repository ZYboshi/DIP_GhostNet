# 基于GhostNet的图像分类技术研究与实践改进

> 西南交通大学 - 数字图像处理课程设计项目 \
> 2022级  2025-2026年 \
> 张彦博

## 📖 项目简介

本项目是数字图像处理的课程设计（以学习论文以及复现为主），主要工作包括：
1.  **模型复现**：基于pytorch框架，复现轻量化神经网络**GhostNet**。
2.  **对比实验**：将其与**MobileNetV3**、**ShuffleNetV2**等主流轻量模型在imagenet的自己数据集上进行性能对比。
（主要原因就在于重新训练很花时间，将这个跑通就可以了）
3.  **模型改进（暂定，可能不做了）**：提出并实现了一种**混合Ghost比例（GhostNet-Hybrid）** 的改进方案，通过在网络不同深度调整特征冗余度，以寻求精度与效率的更好平衡。


## 🛠 环境依赖

```bash
pip install -r requirements.txt
```

## 工程目录说明
```
DIP_GhostNet/
├── dataset/                  # 数据集存放目录
│   ├── cifar-10-batches-py/  # CIFAR-10数据集文件夹
│   └── ImageNet-Mini/        # ImageNet-Mini数据集文件夹
│       └── cifar-10-python.tar.gz  # 数据集压缩包
├── ghost_model/             # GhostNet模型相关文件
│   ├── __init__.py          # 初始化文件
│   ├── ghost_bottleneck.py  # Ghost Bottleneck模块定义
│   ├── ghost_module.py      # Ghost Module模块定义
│   └── ghostnet.py          # GhostNet模型定义
├── ghost_model_checkpoints/ # 模型检查点保存目录
│   └── best_ghostnet_cifar10.pth  # 最佳模型权重文件
├── Notes/                   # 笔记和说明文档目录
│   ├── GhostNet使用.md      # GhostNet模型使用说明
│   ├── timm包使用.md        # timm库使用说明
│   └── 对比实验.md          # 对比实验说明
├── result/                  # 实验结果保存目录
│   ├── final_results.json   # 最终结果JSON文件
│   ├── mobilenetv3_small.csv  # MobileNetV3小模型结果CSV文件
│   ├── predictions_ghostnet.csv  # GhostNet预测结果CSV文件
│   ├── training_plots.png   # 训练过程图表图片
│   ├── training_results.csv  # 训练结果CSV文件
│   └── training_results.json  # 训练结果JSON文件
├── utils/                   # 辅助工具和函数目录
│   ├── __init__.py          # 初始化文件
│   ├── data_process.py      # 针对mobilenetv3和ghostnet_100的数据处理脚本
│   ├── get_dataset.py       # ImageNet-Mini数据集脚本
│   ├── ghostnet_data_loader.py  # GhostNet数据加载器
│   └── model_metrics.py     # 针对自己创建的ghsotnet的模型评估指标脚本
├── args.py                  # 参数配置文件
├── environment.yml          # 环境配置文件
├── eval_all.py              # 评估脚本 -- 主要脚本1
├── ghostnet.py              # GhostNet模型训练  -- 主要脚本2
├── model_analyzer.py        # 模型分析工具
├── models.py                # 模型定义文件
├── README.md                # 项目说明文档

```

## 🚀 快速开始

### 环境配置：
环境安装:

`conda env create --file environment.yml
`

环境激活:

`conda activate dipghostnet
`
### 数据集获取
> 数据集放置目录:dataset\ImageNet-Mini\
> json文件中有其标签

* `cd utils`
* `python get_dataset.py` 
* `cd ..`

根据终端的提示信息，将从kaggle下载的数据集放置到数据集目录

```text
├── dataset/  
    ├──ImageNet-Mini/ 
        ├──images/
        ├──imagenet_class_index.json  

```

### 








